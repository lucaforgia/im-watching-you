import { test } from 'qunit';
// import Ember from 'ember';
import moduleForAcceptance from 'im-watching-you/tests/helpers/module-for-acceptance';

moduleForAcceptance('Acceptance | tasks/show');
test('visiting /tasks/show', function(assert) {
    // let model = this.subject();
    assert.expect(1);


    // beforeEach: function() {
    // };

    // var store = this.application.__container__.lookup('route:tasks').store;
    // var record;
    // var idTask = 1111;
    // Ember.run(()=>{
	//
    //     record = store.createRecord('task',{title:'primo',time:0, id:idTask});
    //     return record.save();
    // });
	//
    // visit(`/tasks/${idTask}`);
    // andThen(function() {
    //     assert.ok(true);
    // });

    assert.ok(true);
});
